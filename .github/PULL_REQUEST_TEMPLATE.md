## What this changes

## Why

## Testing
- [ ] `npm run lint` passes
- [ ] `npm run test` passes
- [ ] `npm run test:e2e` passes (if UI/behavior changed)
- [ ] `npm run build` succeeds

## Scope check
Does this stay within [SCOPE.md](../SCOPE.md) (generator, not tracker — no
accounts, no task-completion tracking, no backend)? If not, explain why.
